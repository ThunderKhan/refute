Verification status: RETRY REQUIRED AFTER THIRD FIX

First local attempt on Windows with `qwen3:0.6b` failed before execution:

`refute verify benchmark\case_001 --provider ollama --model qwen3:0.6b --iteration 2`

Observed error:

`refute: error: reproducer did not return valid JSON`

The batch command failed for the same reason before producing an Iteration 2 benchmark result.

Initial diagnosis and fix:
- the initial Reproducer parser assumed the model response was either clean JSON or a single fenced JSON block;
- small local models can prepend reasoning, `<think>` text, or markdown commentary around an otherwise valid JSON object;
- parsing was changed to scan noisy output for the first decodable JSON object while preserving strict field validation and generated-test safety checks.

Second local attempt after that parser fix still produced the same error for both the single-case and batch commands.

Second diagnosis:
- some `qwen3:0.6b` responses contain no usable JSON object at all, so parser tolerance alone cannot make the response valid;
- a malformed model generation should be treated as a recoverable agent failure inside the bounded Reproducer loop, not as a fatal verification error.

Second fix applied:
- malformed or unsafe Reproducer outputs are now caught as `ReproductionGenerationError`;
- the raw unusable model response is persisted as evidence rather than discarded;
- a concise formatting/validation error is fed back to the next Reproducer attempt;
- the workflow retries within the existing bounded attempt budget;
- if every Reproducer generation is unusable, verification still continues with existing deterministic tests and explicitly tells the Verifier that no reproduction was established;
- the run manifest records `reproduction_generation_failures` and `generation_failure_recovery`;
- tests cover recovery after one malformed generation and graceful continuation when all three generations are malformed.

Third local attempt after malformed-output recovery failed differently:

`refute: error: could not reach language-model provider: timed out`

The batch command failed with the same provider timeout.

Third diagnosis:
- provider timeouts are operational agent failures distinct from malformed structured output;
- the Iteration 2 workflow was only recovering from malformed/unsafe Reproducer responses, while `LLMError` timeouts could still abort the run from Investigator, Reproducer, or Verifier calls.

Third fix applied:
- Investigator provider calls now receive a bounded retry before the run fails;
- Reproducer provider timeouts are recorded as failed generation attempts and consume the existing bounded reproduction attempt budget instead of immediately aborting the run;
- Verifier provider calls now receive a bounded retry;
- provider failures are written to evidence metadata so they remain visible rather than being hidden;
- the run manifest declares `provider_timeout_recovery` when Iteration 2 completes.

Required local checks after pulling the third fix:
1. git pull
2. python -m pytest
3. confirm Ollama is responsive with `ollama list`
4. refute verify benchmark\case_001 --provider ollama --model qwen3:0.6b --iteration 2
5. refute eval-advanced benchmark --provider ollama --model qwen3:0.6b --iteration 2

If `ollama list` itself hangs or errors, restart Ollama before rerunning `refute`; that would indicate a local provider/service problem rather than a verification-pipeline bug.

Expected single-case behavior:
- malformed Reproducer responses no longer terminate the run;
- Reproducer provider timeouts no longer immediately terminate the run;
- up to three generation/execution attempts may occur;
- raw malformed responses and provider failures are preserved in run evidence;
- transient Investigator or Verifier provider failures receive a bounded retry;
- if a valid reproduction is eventually produced, its original/patched execution is recorded;
- if none is produced, the Verifier still runs using the remaining evidence, provided the provider is ultimately reachable.

Expected aggregate evidence paths:
- artifacts/eval/advanced_iteration_2/summary.json
- artifacts/eval/advanced_iteration_2/cases.jsonl
- artifacts/eval/advanced_iteration_2/report.md

Frozen comparison points:
- Baseline v1: 10.0% verdict accuracy, 57.1% false acceptance, 2.405s average runtime
- Advanced Iteration 1: 40.0% verdict accuracy, 28.6% false acceptance, 5.641s average runtime

Do not mark this trace complete until successful local results are supplied by the human.

# Cache may return a stale value after an update

A user reports that a cache lookup occasionally returns the previous value after an update. The report does not include the exact sequence of operations, concurrency conditions, or a reproducible example.
